// const serviceLogin = require('../services/loginService');

// const insertLogin = async (req, res) => {
//     const { email } = req.body;
//     const user = await serviceLogin(email);
//     const result = res.status(user.status).json(user.message);
    
//     return result;
// };

// module.exports = {
//     insertLogin,
// };